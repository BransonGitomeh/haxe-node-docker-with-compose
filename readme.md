# What is this

A setup where you use haxe for backend development, deployed to docker. 

## Try it out
```
$ clone && cd into the dir

# docker-compose
$ docker-compose up

# docker
$ docker build .
```